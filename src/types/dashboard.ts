export type BarChartPoint = {
  labelKey: string;
  value: number;
};

export type TrendSeries = {
  labelKey: string;
  dataKey: string;
};

export type SearchVisibilityTrendPoint = {
  periodKey: string;
  Amazon: number;
  Walmart: number;
  Target: number;
  Northwind: number;
};
